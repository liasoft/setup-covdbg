name: 'Setup covdbg'
description: 'Download and setup covdbg on Windows runners'
author: 'covdbg'
branding:
  icon: 'download'
  color: 'blue'
inputs:
  version:
    description: 'Version of covdbg to download (e.g., 1.3.0, latest)'
    required: true
outputs:
  covdbg-path:
    description: 'Path where covdbg was installed'
runs:
  using: 'node24'
  main: 'dist/index.js'
